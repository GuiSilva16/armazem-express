# URL pública da API do backend (Render). TEM de incluir o /api no fim.
# Exemplo: https://armazem-express-api.onrender.com/api
# Em desenvolvimento deixa vazio — o Vite faz proxy de /api para localhost:4000.
VITE_API_URL=
